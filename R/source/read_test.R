data = scan() # 파이썬 input() 함수와 같다.
data
data[1]

# 문자읽기
data = scan(what="")
data
# 한줄 읽기
data = readline(prompt = '이름 : ')
data
data2 = as.numeric(readline(prompt = '나이 : '))-1
data2

# 쓰기
write()
